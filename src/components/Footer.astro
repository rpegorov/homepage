---
import { t } from '../i18n';
import type { Lang } from '../lib/lang';

interface Props {
  lang: Lang;
}
const { lang } = Astro.props;
---
<footer class="footer">
  <p>{t(lang, 'common.footer.copyright', { year: new Date().getFullYear() })}</p>
</footer>
